import * as React from 'react';
import { PageHeader } from '../components/PageHeader/PageHeader';

export const Profile: React.FunctionComponent = () => {
  return <PageHeader title={'Profile'} description={'Profile.'}></PageHeader>;
};
